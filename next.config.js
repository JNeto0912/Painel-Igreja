// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactCompiler: true, // Certifique-se de que esta linha está removida ou comentada
  reactStrictMode: true, // Pode manter ou remover, é uma boa prática
};

module.exports = nextConfig; // Use module.exports para JS